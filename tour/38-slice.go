package main

import "fmt"

/*
切片的默认行为
在进行切片时, 你可以利用它的默认行为来忽略上下界.
切片下界的默认值为0, 上界则是该切片的长度.
对于数组
var a [10]int
来说, 一下切片是等价的:
a[0: 10]
a[:10]
a[0:]
a[:]
*/
func main() {

	s := [10]int{1, 2, 3, 4, 5, 6, 7}

	s1 := s[0:10]
	s2 := s[:10]
	s3 := s[0:]
	s4 := s[:]

	fmt.Println(s1, s2, s3, s4)

}
